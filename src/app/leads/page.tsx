import React, { FC, ReactElement } from "react";
import Box from "@mui/material/Box";

interface LeadsPageProps {}

const LeadsPage: FC<LeadsPageProps> = (): ReactElement => {
  return <Box>LeadsPage</Box>;
};

export default LeadsPage;
