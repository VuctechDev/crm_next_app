import React, { FC, ReactElement } from "react";
import Box from "@mui/material/Box";

interface FilePickerWrapperProps {}

const FilePickerWrapper: FC<FilePickerWrapperProps> = (): ReactElement => {
  return <Box>FilePickerWrapper</Box>;
};

export default FilePickerWrapper;
